{% extends 'base.html.twig' %}

{% block title %}TemplateCategory index{% endblock %}

{% block body %}
    <h1>TemplateCategory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for template_category in template_categories %}
            <tr>
                <td>{{ template_category.id }}</td>
                <td>{{ template_category.name }}</td>
                <td>
                    <a href="{{ path('design_template_category_show', {'id': template_category.id}) }}">show</a>
                    <a href="{{ path('design_template_category_edit', {'id': template_category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('design_template_category_new') }}">Create new</a>
{% endblock %}
