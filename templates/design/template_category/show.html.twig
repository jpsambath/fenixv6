{% extends 'base.html.twig' %}

{% block title %}TemplateCategory{% endblock %}

{% block body %}
    <h1>TemplateCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ template_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ template_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('design_template_category_index') }}">back to list</a>

    <a href="{{ path('design_template_category_edit', {'id': template_category.id}) }}">edit</a>

    {{ include('design/template_category/_delete_form.html.twig') }}
{% endblock %}
