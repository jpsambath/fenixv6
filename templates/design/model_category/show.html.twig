{% extends 'base.html.twig' %}

{% block title %}ModelCategory{% endblock %}

{% block body %}
    <h1>ModelCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ model_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ model_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('design_model_category_index') }}">back to list</a>

    <a href="{{ path('design_model_category_edit', {'id': model_category.id}) }}">edit</a>

    {{ include('design/model_category/_delete_form.html.twig') }}
{% endblock %}
